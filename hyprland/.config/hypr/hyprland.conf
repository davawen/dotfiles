
# #######################################################################################
# AUTOGENERATED HYPR CONFIG.
# PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
# OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################

autogenerated = 0 # remove this line to remove the warning

# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/Configuring-Hyprland/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf


################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/

$output1 = HDMI-A-1
$output2 = DP-1

monitor=$output1,1920x1080@144,1920x120,1
monitor=$output2,1920x1200@60,0x0,1

workspace=1,monitor:$output1
workspace=2,monitor:$output2
workspace=3,monitor:$output1
workspace=4,monitor:$output2
workspace=5,monitor:$output1
workspace=6,monitor:$output2
workspace=7,monitor:$output1
workspace=8,monitor:$output2

###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = kitty
$fileManager = dolphin
$menu = keal

#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

# exec-once = $terminal
exec-once = nm-applet --indicator
exec-once = waybar
exec-once = mako --ignore-timeout 1 --default-timeout 3000
# exec-once = hyprpaper & firefox
exec-once = hyprpm reload -n


#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general { 
    gaps_in = 5
    gaps_out = 5

    border_size = 2

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false 

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = hy3
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 5

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1.0

    drop_shadow = true
    shadow_range = 4
    shadow_render_power = 3
    col.shadow = rgba(1a1a1aee)

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 3
        passes = 1
        
        vibrancy = 0.1696
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = true

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05
	bezier=overshot,0.55,0.82,0.1,1.3

    animation = windows, 1, 7, myBezier, popin
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default, slide
}

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc { 
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background. :(
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us(altgr-intl)
    kb_variant =
    kb_model =
    kb_options = caps:escape
    kb_rules =

    follow_mouse = 1

	accel_profile = flat
    sensitivity = -0.7 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = false
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
gestures {
    workspace_swipe = false
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
# device {
#     name = epic-mouse-v1
#     sensitivity = -0.5
# }

#######
# HY3 #
######

plugin {
	hy3 {
		# disable gaps when only one window is onscreen
		# 0 - always show gaps
		# 1 - hide gaps with a single window onscreen
		# 2 - 1 but also show the window border
		no_gaps_when_only = 1 # default: 0

		# policy controlling what happens when a node is removed from a group,
		# leaving only a group
		# 0 = remove the nested group
		# 1 = keep the nested group
		# 2 = keep the nested group only if its parent is a tab group
		node_collapse_policy = 0 # default: 2

		# offset from group split direction when only one window is in a group
		group_inset = 10 # default: 10

		# if a tab group will automatically be created for the first window spawned in a workspace
		tab_first_window = false

		# tab group settings
		tabs {
			# height of the tab bar
			height = 18 # default: 15

			# padding between the tab bar and its focused node
			padding = 3 # default: 5

			# the tab bar should animate in/out from the top instead of below the window
			from_top = false # default: false

			# rounding of tab bar corners
			rounding = 3 # default: 3

			# render the window title on the bar
			render_text = true # default: true

			# center the window title
			text_center = false # default: false

			# font to render the window title with
			text_font = Iosevka # default: Sans

			# height of the window title
			text_height = 12 # default: 8

			# left padding of the window title
			text_padding = 3 # default: 3

			# active tab bar segment color
			col.active = 0xff32b4ff # default: 0xff32b4ff

			# urgent tab bar segment color
			col.urgent = 0xffff4f4f # default: 0xffff4f4f

			# inactive tab bar segment color
			col.inactive = 0x80808080 # default: 0x80808080

			# active tab bar text color
			col.text.active = 0xff000000 # default: 0xff000000

			# urgent tab bar text color
			col.text.urgent = 0xff000000 # default: 0xff000000

			# inactive tab bar text color
			col.text.inactive = 0xff000000 # default: 0xff000000
		}

		# autotiling settings
		autotile {
			# enable autotile
			enable = false # default: false

			# make autotile-created groups ephemeral
			ephemeral_groups = true # default: true

			# if a window would be squished smaller than this width, a vertical split will be created
			# -1 = never automatically split vertically
			# 0 = always automatically split vertically
			# <number> = pixel height to split at
			trigger_width = 0 # default: 0

			# if a window would be squished smaller than this height, a horizontal split will be created
			# -1 = never automatically split horizontally
			# 0 = always automatically split horizontally
			# <number> = pixel height to split at
			trigger_height = 0 # default: 0

			# a space or comma separated list of workspace ids where autotile should be enabled
			# it's possible to create an exception rule by prefixing the definition with "not:"
			# workspaces = 1,2 # autotiling will only be enabled on workspaces 1 and 2
			# workspaces = not:1,2 # autotiling will be enabled on all workspaces except 1 and 2
			workspaces = all # default: all
		}
	}
}

####################
### KEYBINDINGSS ###
####################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
# bind = $mod, M, exit,
bind = $mod, return, exec, $terminal
bind = $mod, d, exec, $menu

bind = $mod, q, killactive,
bind = $mod, space, togglefloating,
bind = $mod, f, fullscreen

# Move focus with mod + hjkl keys
bind = $mod, h, hy3:movefocus, l
bind = $mod, j, hy3:movefocus, d
bind = $mod, k, hy3:movefocus, u
bind = $mod, l, hy3:movefocus, r

# Move focus with mod + hjkl keys
bind = $mod SHIFT, h, hy3:movewindow, l
bind = $mod SHIFT, j, hy3:movewindow, d
bind = $mod SHIFT, k, hy3:movewindow, u
bind = $mod SHIFT, l, hy3:movewindow, r

bind = $mod, c, hy3:makegroup, h
bind = $mod, v, hy3:makegroup, v

bind = $mod, w, hy3:changegroup, tab
bind = $mod, e, hy3:changegroup, opposite

bind = $mod, a, hy3:changefocus, raise

# Switch workspaces with mod + [0-9]
bind = $mod, 1, workspace, 1
bind = $mod, 2, workspace, 2
bind = $mod, 3, workspace, 3
bind = $mod, 4, workspace, 4
bind = $mod, 5, workspace, 5
bind = $mod, 6, workspace, 6
bind = $mod, 7, workspace, 7
bind = $mod, 8, workspace, 8
bind = $mod, 9, workspace, 9
bind = $mod, 0, workspace, 10

# Move active window to a workspace with mod + SHIFT + [0-9]
bind = $mod SHIFT, 1, movetoworkspace, 1
bind = $mod SHIFT, 2, movetoworkspace, 2
bind = $mod SHIFT, 3, movetoworkspace, 3
bind = $mod SHIFT, 4, movetoworkspace, 4
bind = $mod SHIFT, 5, movetoworkspace, 5
bind = $mod SHIFT, 6, movetoworkspace, 6
bind = $mod SHIFT, 7, movetoworkspace, 7
bind = $mod SHIFT, 8, movetoworkspace, 8
bind = $mod SHIFT, 9, movetoworkspace, 9
bind = $mod SHIFT, 0, movetoworkspace, 10

bind = $mod, tab, workspace, m+1
bind = $mod SHIFT, tab, workspace, m-1

# Example special workspace (scratchpad)
bind = $mod, S, togglespecialworkspace, magic
bind = $mod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mod + scroll
bind = $mod, mouse_down, workspace, e+1
bind = $mod, mouse_up, workspace, e-1

# Move/resize windows with mod + LMB/RMB and dragging
bindm = $mod, mouse:272, movewindow
bindm = $mod, mouse:273, resizewindow

# Volume control
exec-once = playerctld daemon

bind = ,XF86AudioRaiseVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ +5% # Raise
bind = ,XF86AudioLowerVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ -5% # Lower
bind = ,XF86AudioMute, exec, pactl set-sink-mute @DEFAULT_SINK@ toggle # Mute

# Sreen brightness controls -- # https://github.com/haikarainen/light
bind = ,XF86MonBrightnessUp, exec, blight set +5%    # increase screen brightness
bind = ,XF86MonBrightnessDown, exec, blight set -5%  # decrease screen brightness

# multimedia keys
bind = ,XF86AudioPlay,  exec, playerctl play-pause
bind = ,XF86AudioStop,  exec, playerctl stop
bind = ,XF86AudioNext,  exec, playerctl next
bind = ,XF86AudioPrev,  exec, playerctl previous
bind = ,XF86AudioPause, exec, playerctl pause

$scripts = /home/davawen/.config/sway/scripts/

bind = ,Print, exec, $scripts/screen.sh 
bind = SHIFT,Print, exec, $scripts/screen.sh "edit"
bind = CTRL,Print, exec, grimshot copy output


##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule v1
# windowrule = float, ^(kitty)$

# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$

windowrulev2 = suppressevent maximize, class:.* # You'll probably like this.

windowrulev2 = float,class:steam,title:^(?!^Steam$).*$
windowrulev2 = float,title:^ProtonUp-Qt - Wine/Proton Installer$
windowrulev2 = float,class:Tk,title:^Python Turtle Graphics$

# windowrulev2 = [window_role="pop-up"] float enable
# windowrulev2 = [window_role="bubble"] float enable
# windowrulev2 = [window_role="task_dialog"] float enable
# windowrulev2 = [window_role="Preferences"] float enable

# windowrulev2 = [window_role="task_dialog|bubble|page-info|Preferences|pop-up"] float enable
# windowrulev2 = [window_role="Open Files"] float enable, sticky enable
# windowrulev2 = [window_role="File Operation Progress"] float enable, sticky enable
# windowrulev2 = [window_role="Save As"] float enable; resize set widdth 50ppt height 50ppt

# windowrulev2 = [window_type="dialog"] float enable
# windowrulev2 = [window_type="menu"] float enable
windowrulev2 = float,class:flameshot
windowrulev2 = float,class:PlayOnLinux
windowrulev2 = float,title:Event Tester
# windowrulev2 = [app_id="firefox" title="Sharing Indicator"] float enable

windowrulev2 = float,title:Keal
windowrulev2 = float,title:xeyes

# Sticky window
#windowrulev2 = [instance="file_progress"]  sticky enable
#windowrulev2 = [class="info|Mate-color-select|gcolor2|timesup|QtPass|GtkFileChooserDialog"] sticky enable

# Focus window settings
# no_focus [window_role="pop-up"]
# focus_on_window_activation focus 
